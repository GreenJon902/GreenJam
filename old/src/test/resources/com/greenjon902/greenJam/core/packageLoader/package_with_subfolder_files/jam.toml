name = "package_with_subfolder_files"
description = "A simple example of a package with files and modules"
authors = ["GreenJon902"]

[Loader]
file-regex = ["([^/]+?)\\.jam", "ext\\/([^/]+?)\\.jam"]
module-regex = "^((?!ext(?:(?:/?)|$))[^/]+)"